# Invoice–Payment–Journal Flow Audit Handover (TH + GT)

Date: 2026-08-03
Scope: Tien Hock and Green Target accounting Invoice → Payment → Journal flow. Jelly Polly excluded (accounting outsourced).
Method: static code review of `src/routes/**` and `src/pages/**` (three parallel audit passes: TH invoice/payment, TH receipt/bank-in/journal, GT full flow). **Caveat:** the dev DB/Docker was down during the audit, so DB-constraint claims (e.g. a TH journal-line shape CHECK) and live data states are unverified; everything else is confirmed in code.

Status legend: **FIXED** (this pass, 2026-08-03) · **DEFERRED** (deliberately not done now) · **OPEN** (not yet scheduled)

---

## 1. Critical findings (ledger/money integrity)

| # | Finding | Citation | Status |
|---|---------|----------|--------|
| C1 | **GT operational accounting routes had no session auth.** `src/routes/index.js:221` only blankets TH `/api` with `authMiddleware`; GT invoices/payments/adjustment-docs (all journal-posting since G7/GT-P1) plus financial-reports, bank-statement and debtors were mounted unauthenticated. Any unauthenticated client could mutate the GT ledger and read GT financials. | `src/routes/index.js:292-393` | FIXED |
| C2 | **TH bank-in service has no accounting-period lock** — `createBankIn` / `createDrawingJournal` / `cancelBankIn` never call `assertTienHockAccountingDateUnlocked`, and the BankInPage date pickers allow any date incl. future. The one unguarded write into locked Jan–May history. | `src/routes/accounting/bank-in-service.js:192-199, 485-492, 566-570`; `src/pages/Accounting/BankInPage.tsx:497-508, 419-430` | **DEFERRED — user decision 2026-08-03: "not gonna happen at all".** Revisit if multi-user keying starts. |
| C3 | **TH payment-type conversion and line-item edit lacked cancelled-invoice and active-adjustment guards.** Both paths check only pending cheques; on a cancelled invoice they could flip `invoice_status` back to `paid` and re-post the S journal (pencils were visible on cancelled invoices). Editing an invoice with an active CN/DN restored the pre-CN balance while the CN journal stayed posted — every other mutation path blocks on active adjustments. | `src/routes/sales/invoices/invoices.js:4748-4948` (paymenttype), `:3589-3901` (order-details); `src/pages/Invoice/InvoiceDetailsPage.tsx:2111-2118, 2319-2329` | FIXED |
| C4 | **Receipt cancellation didn't protect a consumed overpayment excess.** `cancelReceipt`/`cancelReceiptGroup` never checked `applied_amount`/`refunded_amount` on the receipt's excess allocations. Sequence: overpayment → excess applied (DR CUST_DEP / CR debtor) → cancel original receipt → funding CR CUST_DEP cancelled while application stays posted → CUST_DEP negative, invoices paid with money that "never arrived". | `src/routes/accounting/receipt-service.js:1022-1113, 1234-1344` | FIXED |
| C5 | **TH invoice cancellation leaked consumed overpayment.** `DELETE /:id` cancelled overpayment application payments and their journals but never reversed the FIFO consumption in `overpayment_applications` / `receipt_allocations.applied_amount`; the customer's unapplied excess stayed permanently understated. The single-payment cancel endpoint already did this correctly (`payments.js:879-893`). | `src/routes/sales/invoices/invoices.js:3348-3371` | FIXED |
| C6 | **Source-owned TH journals could be cancelled directly from the Journal page.** The cancel route had no `source_type` check; cancelling a receipt REC left the receipt posted/invoice paid with the money gone from the ledger; cancelling a bank-in RV left pools consumed with no bank debit; cancelling an invoice S was silently re-posted by the next invoice edit. GT already blocked this. | `src/routes/accounting/journal-entries.js:1279-1338`; `src/pages/Accounting/JournalDetailsPage.tsx:415, 538-549` | FIXED |
| C7 | **The OTHER GT operational mounts are still unauthenticated — same vulnerability class as C1.** GT payroll, customers, rentals, einvoice (and any other `/greentarget/api/*` mount not named in C1) have no `authMiddleware`, so GT payroll/staff data, customer records and rental operations are readable/writable without a session. Left unfixed on 2026-08-03 only because it was outside the accounting audit scope — closing it is the same one-line-per-mount pattern used for C1 (`authMiddleware(pool)`, plus `checkRestoreState` on mutating mounts, mirroring the existing authed GT mounts in `src/routes/index.js`). Watch for: GT payroll routes share `public.staffs` data (a scoping decision), and any genuinely public endpoint must be exempted the way customer-signups was. **User asked on 2026-08-03 that this stay tracked.** | `src/routes/index.js` (all `/greentarget/api/*` mounts other than invoices/payments/adjustment-docs/financial-reports/bank-statement/debtors/journal-entries/account-codes/journal-vouchers/customer-signups) | OPEN |

## 2. Medium findings

| # | Finding | Citation | Status |
|---|---------|----------|--------|
| M1 | **Audit columns silently broken.** `authMiddleware` populates only `req.session`; `req.staffId`/`req.user` are never set, so `created_by`/`posted_by`/`cancelled_by` were NULL on every manual journal, receipt, bank-in and payment in both companies. Related: `auth.js:84` self-join typo (`ON s.staff_id = s.staff_id`) made session staff name/job come from an arbitrary staff row. | `src/middleware/auth.js:84, 116-128`; consumers `journal-entries.js:951, 1157, 1260, 1322, 1449`, `receipts.js:30`, `payments.js:419+`, `bank-ins.js:172+`, GT `payments.js:1035+` | FIXED |
| M2 | **Line-item edit drops invoice rounding.** Edit recomputes `newTotal = round(subtotal + tax)` and never touches `invoices.rounding`; a rounded invoice's totals stop adding up on screen and in the journal. | `src/routes/sales/invoices/invoices.js:3717-3721, 3808-3821`; create path `InvoiceFormPage.tsx:333-334` | OPEN |
| M3 | **Batch submission swallows journal failures.** In `POST /submit-invoices` a non-period-lock `syncSalesJournalEntry` failure is only `console.error`'d — the sale silently never reaches the GL. | `src/routes/sales/invoices/invoices.js:2779-2789` | OPEN |
| M4 | **Editing a system journal destroys receipt line metadata with no warning.** Journal PUT re-inserts lines without `display_reference`/`cheque_reference`/`display_order` and sets `manual_override`; receipt journals are create-once, so nothing rebuilds the lost legacy refs. | `src/routes/accounting/journal-entries.js:1161-1184` | PARTIAL — pre-save warning + confirmation added this pass; metadata preservation itself still OPEN |
| M5 | **CH_REV1 pool arithmetic ignores manual journals.** `getCashSalesPools` only counts `entry_type='S'` journals; a manual CH_REV1 correction isn't netted, so Bank-In can offer banking more than the account holds. | `src/routes/accounting/bank-in-service.js:90-99, 297-318` | OPEN |
| M6 | **Full payment allowed while an equivalent cheque is pending** on the inline InvoiceDetailsPage payment form; the later cheque confirmation then hard-fails, leaving a stuck pending cheque. | `src/pages/Invoice/InvoiceDetailsPage.tsx:2159-2313`; `receipt-service.js:182-189` | OPEN |
| M7 | **GT: sales journals adopted by reference keep `source_type NULL`**, bypassing GT's source-owned cancel guard; cancelling one strands the invoice (payments and edits then 409). | `src/routes/greentarget/accounting/sales-journal.js:438-445`; `posting-utils.js:215-231` | OPEN |
| M8 | **GT: journal PUT guards only the submitted date, not the stored one** (TH's fix guards both). | `src/routes/greentarget/accounting/journal-entries.js:721-724` | OPEN |

## 3. UI/UX improvements

| # | Item | Citation | Status |
|---|------|----------|--------|
| U1 | Confirmation dialog on payment-type conversion (comparably destructive to cancellation, which already has one) | `src/pages/Invoice/InvoiceDetailsPage.tsx:3375-3381` | FIXED |
| U2 | Held overpayment auto-applied in full by default → changed to opt-in | `src/components/Invoice/PaymentForm.tsx:502-512` | FIXED |
| U3 | Hide edit pencils (customer/salesman/datetime/payment-type/line-items) on cancelled invoices | `src/pages/Invoice/InvoiceDetailsPage.tsx:2046-2053, 2069-2076, 2093-2100, 2111-2118, 2319-2329` | FIXED |
| U4 | Warn when line-item edit cancels legacy pending payments (response carries `paymentInfo.pendingCancelled`, UI ignored it) | `src/pages/Invoice/InvoiceDetailsPage.tsx:1100-1162` | FIXED |
| U5 | Annotate/pre-disable Cancel & Edit on source-owned journals; pre-save detach confirmation on journal edit | `src/pages/Accounting/JournalDetailsPage.tsx`, `src/pages/Accounting/JournalEntryPage.tsx:1405` | FIXED |
| U6 | Cheque-clearance date pickers: min = `received_date`, default `max(today, received_date)` | `src/components/Invoice/ReceiptDetailsDialog.tsx`, `src/components/Invoice/PaymentTable.tsx` (+ the inline confirm dialogs in TH/JP `InvoiceDetailsPage.tsx`) | FIXED |
| U7 | GT invoice-created-but-payment-failed toast now directs user to re-record from invoice details | GT `InvoiceFormPage.tsx:1107-1110` | FIXED |
| U8 | Bank-In date pickers min-bound (ties to C2) | `src/pages/Accounting/BankInPage.tsx` | DEFERRED with C2 |
| U9 | "Pre-June opening cash" hint on BankInPage is displayed but unreachable — needs product decision (add opening-pool input vs remove hint) | `src/pages/Accounting/BankInPage.tsx:553-565` | OPEN |
| U10 | 409 period-lock/reference errors arrive as transient toasts; blocking server errors deserve persistent inline surfacing | e.g. `JournalEntryPage.tsx:1424-1430` | OPEN |

## 4. Trivial / low-impact (genuine but small; fix opportunistically)

- REC reference and GT adjustment doc-id generators race under concurrency (`FOR UPDATE SKIP LOCKED` reads a stale max) — one request dies on the unique constraint with a raw pg error instead of retrying. `src/routes/accounting/payment-journal.js:21-44`; `src/routes/greentarget/adjustment-docs.js:57-77`.
- Double-submitted **pending cheques** have no server-side dedupe (no journal → no unique-reference backstop); posted full-balance payments are protected, partials aren't. `src/routes/sales/receipts.js:28-126`.
- `PaymentTable.tsx:399, 439-440` uses `String(date).slice(0,10)` — rule-17 day-before pattern; currently harmless (uniform shift, grouping only). `toDayString` (line 64) already does it right.
- `cancelReceipt` status restore loses a prior `Overdue` (maps to `Unpaid`); the payments.js path preserves it (`payments.js:936-942`). `receipt-service.js:1303`.
- GT `adjustment-docs.js:843` uses `toISOString().slice(0,10)` (UTC date) for consolidated-wrapper `date_issued` — wrong day before 8am KL; no journal impact.
- GT debtor-registry effective-range checked against invoice date rather than posting date — latent trap, no current data instance. `greentarget/accounting/sales-journal.js:284-299`.
- GT invoice form still exposes a `tax_amount` input even though GT journals post gross — keyed tax silently becomes TGA revenue. GT `InvoiceFormPage.tsx:1709-1716`.
- Journal balance tolerance allows off-by-one-sen posted journals (`journal-entries.js:892, 1036, 1246`); GT has a line-shape CHECK, TH's unverified (dev DB down).
- Bank-In cancel dialog has no double-click guard (`BankInPage.tsx:336-354`).
- Legacy pending-cheque confirmation can hit the create-form's imported-match 409 with an inapplicable message (`payments.js:632-646` → `receipt-service.js:339-381`).
- `PUT /:id/datetime` has no cancelled-invoice check (`invoices.js:4951`) — sync short-circuits safely; only edits an audit record's date.
- GT: check-then-insert races surface raw pg errors (custom invoice number, journal duplicate reference); `invoice_number` varchar(20) and journal `reference_no` lack length validation.
- GT `nextGTPostingSequence` reads MAX+1 without a lock — concurrent posts can duplicate `posting_sequence` (print order only). `posting-utils.js:66-74`.
- GT `POST /debtor-subledger` not transactional with the triggering invoice save — an identity can survive a failed invoice POST (harmless orphan registry row).
- Deprecated `DELETE /api/payments/:id` rewrites `req.method`/`req.url` and re-enters the router (`payments.js:1000-1015`) — works, fragile.
- `resolveGTJournalSource`'s `"payment"` case is dead code post-GT-P1 (`greentarget/accounting/journal-entries.js:92-113`).

## 5. Doc notes

- The GT-P7 paragraph in AGENTS.md ("CD_SD fallback / debtor field not required") is superseded by the 2026-08-01 debtor-dimension design (registry identity per invoice, CD_SD non-selectable) — code is the newer design.
- Verified sound during the audit (no action): invoice row-locking + per-invoice over-settlement caps; atomic header+allocations+journal in one transaction; RV number reservation race handled by unique constraint; bank-in over-banking re-checked under advisory + row locks; cheque clearance date validation (≥ received, ≤ today, never defaulted); double-submit guards on payment/receipt/bank-in post buttons; GT pending-cheque visibility and receipt-join panel.

## 6. Fix log (2026-08-03)

Fixed this pass (per AGENTS.md rule 10, no build/typecheck run — user tests manually; all edits verified by `node --check` and re-reading):

- **C1 — `src/routes/index.js`:** `authMiddleware(pool)` + `checkRestoreState` on `/greentarget/api/invoices`, `/payments`, `/adjustment-docs`; auth-only on `/financial-reports`, `/bank-statement`, `/debtors` (mirrors the existing authed GT mounts). Public customer-signup submit and the API-key excel export verified unaffected. **Tracked as finding C7 (OPEN, user-flagged for follow-up):** the other GT operational mounts (payroll, customers, rentals, einvoice, etc.) remain unauthenticated — same one-line pattern applies, but GT payroll shares `public.staffs` data so it was left as a separate scoping decision.
- **C3 — `src/routes/sales/invoices/invoices.js`:** `PUT /:id/paymenttype` and `PUT /:id/order-details` now refuse cancelled invoices (400) and invoices with active non-wrapper adjustment documents (400, citing the doc refs), before any credit/journal mutation.
- **C5 — same file, `DELETE /:id`:** cancelled `overpayment` payments now reverse their `overpayment_applications` (decrement `receipt_allocations.applied_amount` via `GREATEST(0, ...)`, delete application rows) inside the cancel transaction. **Divergence to note:** the single-payment cancel endpoint (`payments.js:879-893`) decrements but does NOT delete the application rows — harmless today (as-of reads filter applications by active payments) but the two paths now differ; align later if desired.
- **C4 — `src/routes/accounting/receipt-service.js`:** `cancelReceipt` and `cancelReceiptGroup` block when any excess allocation of the receipt(s) has `applied_amount`/`refunded_amount` > 0 (plain 400 like the neighboring guards; set `error.status = 409` if 409 semantics are preferred — the routes honor it).
- **C6 — `src/routes/accounting/journal-entries.js`:** `POST /:id/cancel` refuses `source_type !== null` with a structured 409 pointing at the owning document (same shape as GT's guard). Restore endpoint unchanged; source cascades verified to cancel journals via their own service functions, not this handler.
- **M1 — `src/middleware/auth.js`:** self-join typo fixed (`st.id = s.staff_id`); middleware now sets `req.staffId` and `req.user = { id }` from `session.staff_id` (additive; both existing session shapes `req.session.staff.id` and `req.session.staff_id` untouched).
- **U1/U3/U4 — `src/pages/Invoice/InvoiceDetailsPage.tsx`:** all five edit pencils hidden when `isCancelled`; payment-type conversion now goes through a `ConfirmationDialog` stating the from→to consequences; `paymentInfo.paymentsAdjusted.pendingCancelled` surfaces as a toast after line-item edits.
- **U2 — `src/components/Invoice/PaymentForm.tsx`:** overpayment application seeds to 0/unticked (opt-in).
- **U5 — `src/pages/Accounting/JournalDetailsPage.tsx` + `JournalEntryPage.tsx`:** Cancel disabled (and Edit disabled for GT) on source-owned journals with explanatory tooltips; TH journal edit of a not-yet-detached system journal requires a "Save & Detach" confirmation warning about detachment and loss of per-line receipt/cheque references.
- **U6 — `src/components/Invoice/ReceiptDetailsDialog.tsx`, `src/components/Invoice/PaymentTable.tsx`:** clearance pickers min-bounded by received_date, defaulting to max(today, received_date) via `TimeNavigator.minDate` (rule-17 string compares). **Scope addition:** the same fix was applied to the inline confirm dialogs in `src/pages/Invoice/InvoiceDetailsPage.tsx` and `src/pages/JellyPolly/InvoiceDetailsPage.tsx` (same defect, same component).
- **U7 — `src/pages/GreenTarget/Invoices/InvoiceFormPage.tsx`:** payment-failed toast now directs the user to record the payment later from the invoice details page.
- Changelog entries added (2 entries, 2026-08-03) and AGENTS.md updated (hardening bullet, receipt_allocations excess-cancel note, GT auth bullet, GT-P7 superseded marker).

Deferred by user decision: **C2/U8** (bank-in period lock + picker bounds — "not gonna happen at all"; revisit if multi-user keying starts). Everything else remains OPEN/DEFERRED as marked in sections 2–4.

---

## 7. Continuation audit (2026-08-15)

Method: four parallel static re-audit passes over the current code (TH invoice/payment, TH receipt/bank-in/journal, GT full flow, and all flow-touching commits since 2026-08-03). **The 2026-08-03 DB caveat is resolved:** the dev DB (`tienhock_dev_db`) was up this pass and was queried directly. Sections 1–6 above remain the 2026-08-03 record; this section supersedes their statuses where noted.

### 7.1 DB-verified facts (previously unverified)

- `public.journal_entry_lines` **does** carry the line-shape CHECK `chk_debit_or_credit` (exactly one of debit/credit positive, **or both 0**) — same shape as GT's `gt_chk_debit_or_credit`. Both schemas therefore allow 0/0 informational lines by design; the 1-sen journal imbalance gap (§4, tolerance 0.01) is a balance-tolerance issue, not a line-shape one.
- `payments.payment_date` is `timestamp without time zone` → `db-pool.js` re-tags with `'Z'`, so the `String(date).slice(0,10)` grouping in `PaymentTable.tsx:473, 513-514` yields the correct local date. The rule-17 hazard is confirmed **benign** here (downgrade from §4 note).
- `greentarget.invoices.invoice_number` is `varchar(20) UNIQUE`; `greentarget.journal_entries.reference_no` is `varchar(50) UNIQUE` — the §4 race/length findings have a DB backstop (raw pg error, no corruption).
- Dev DB spot-checks: 0 invoice-linked and 0 receipt-linked TH journals lack `source_type`; GT has exactly ONE live `source_type='payment'` journal (id 1708, `REC-197` / `RV26/07/01`, the G7 backfill) — so `resolveGTJournalSource`'s `"payment"` case (`greentarget/accounting/journal-entries.js:96-117`) is **reachable legacy, not dead code** (corrects the §4 note).

### 7.2 Re-verification of sections 1–4 against current code

All 2026-08-03 fixes still hold (journal cancel refuses non-NULL `source_type`, `journal-entries.js:1429-1435`; excess-allocation cancel guards, `receipt-service.js:1464-1477, 1662-1675`; invoice DELETE reverses `overpayment_applications`, `invoices.js:3425-3443`; paymenttype/order-details cancelled+adjustment refusals, `invoices.js:4906, 4913-4929, 3713, 3720-3737`). The new Journal cancel dialog (`JournalDetailsPage.tsx:567-583`) keeps the source-owned disable + tooltip.

| Item | 2026-08-15 status | Notes (current citations) |
|------|-------------------|---------------------------|
| C2 (bank-in period lock) | still DEFERRED/absent | No `assertTienHockAccountingDateUnlocked` anywhere in `bank-in-service.js`/`bank-ins.js`; pickers still unbounded (`BankInPage.tsx:429, 512`) |
| C7 (other GT mounts unauthed) | **PARTIALLY FIXED** | Newly authed since: `sales-report` (`index.js:308`), `opening-balances` (`:399`). Still unauthenticated (writable ones bold in risk terms): **customers, rentals, payroll-employees, employee-pay-codes, monthly-payrolls, monthly-work-logs, employee-payrolls, pinjam-records, mid-month-payrolls, incentives, others-records, daily-lori-habuk, leave-management, e-caruman**, plus read-mostly dashboard/locations/dumpsters/einvoice/pickup-destinations/payroll-rules/salary-report/ledger-types. For comparison: JP has auth only on `/jellypolly/api/account-ledger`; all other ~25 JP mounts are open (out of scope, same class) |
| M2 (rounding dropped on line edit) | **FIXED 2026-08-18** | Edit now carries the keyed rounding: `total = subtotal + tax + rounding` (`invoices.js:3830-3838`); same fix applied to the JP clone (`jellypolly/invoices.js:1598-1602`) |
| M3 (batch submit swallows journal failure) | still OPEN | `invoices.js:2835-2849`; comment claims "self-heal later", no such mechanism exists |
| M4 (journal PUT drops line metadata) | still OPEN (disclosed) | PUT still DELETEs/re-inserts lines without `display_reference`/`cheque_reference`/`display_order` (`journal-entries.js:1281-1304`); edit page now warns explicitly (`JournalEntryPage.tsx:2138`) |
| M5 (CH_REV1 pool ignores manual journals) | **PARTIALLY FIXED** | `getCashSalesPools` now counts `entry_type IN ('S','REC')` (`bank-in-service.js:94-103`) paired with the new `cash-holding-account.js` (same-day cash receipts debit CH_REV1). Residual: manual `J`/`C`/`B` corrections against CH_REV1 still not netted |
| M6 (full payment while cheque pending) | **FIXED 2026-08-18 (net-out)** | `PENDING_SETTLEMENT_SQL` (pending receipt allocations + legacy standalone pending payments, no double-count via `receipt_allocation_id IS NULL`) is now subtracted in `SETTLEABLE_AMOUNT_SQL` (`receipt-service.js:54-107`) and in `getSettleableAmounts` with a self-exclusion for the receipt being confirmed (`lockInvoices(client, allocs, excludeReceiptId)`, confirm path passes its own id). Refusal messages name the uncleared cheque. UI: invoice detail exposes `pending_settlement`, the inline form caps at the netted amount with an amber hint (`InvoiceDetailsPage.tsx`). Side effect: double-submitted pending cheques (§4 dedupe item) are now refused at creation — the first pending cheque nets the settleable to 0. Known remaining edge: `applyOverpayment` caps at raw `balance_due` (not netted), so an overpayment application can still land on a pending-covered invoice |
| M7 (GT adoption keeps source_type NULL) | still OPEN | `sales-journal.js:640-647` → `replaceGTJournal` (`posting-utils.js:217-233`) never sets source; `payments.js:250-256` proves the shape is expected/live |
| M8 (GT PUT guards submitted date only) | **FIXED 2026-08-18** | GT PUT now asserts both stored and submitted dates, mirroring TH's wording (`greentarget/accounting/journal-entries.js:877-885`) |
| U9 (opening-cash hint unreachable) | still OPEN | `BankInPage.tsx:567-578`; server supports pre-cutover `source_dates` (`bank-in-service.js:303-321`), UI can't reach them |
| U10 (409s as transient toasts) | still OPEN | `JournalEntryPage.tsx:1493-1504` |
| §4 trivial items | mostly unchanged | REC ref race (`payment-journal.js:21-44`), 0.01 tolerance (`journal-entries.js:987, 1131, 1366`), Bank-In cancel no double-click guard (`BankInPage.tsx:344-364`, `isConfirming` prop exists but unused), pending-cheque dedupe absent (`receipts.js:28-126`), `cancelReceipt` still maps Overdue→Unpaid (`receipt-service.js:1705`), deprecated DELETE re-enter (`payments.js:1046-1062`), legacy-confirm inapplicable 409 message (`receipt-service.js:494-536` runs unconditionally for the `payments.js:640-693` conversion), GT doc-id race (`adjustment-docs.js:57-77`), GT `toISOString` wrapper date (`adjustment-docs.js:843`), GT registry range vs invoice date (`sales-journal.js:486-501` + `payment-journal.js:147-150`), GT tax_amount input survives the invoice_lines redesign (`InvoiceFormPage.tsx:1904-1930`), GT check-then-insert races, `nextGTPostingSequence` unlocked (`posting-utils.js:66-74`), non-transactional `/debtor-subledger` (`GTInvoiceAccountFields.tsx:463-468`) |

### 7.3 New-code audit (2026-08-03 → 2026-08-15) — verified sound

- **Split tender (68f1e164):** per-leg posting correct — tenders must sum exactly to `totalamountpayable` incl. rounding (`saleTenders.js:71-97`); one receipt per non-cash tender; cash remainder auto-collected; `resyncCashBillSalesJournals` rebuilds the S journal under per-date advisory locks with a non-negative pool post-check; tender-leg cancellation returns the amount to the auto-collection. `lockInvoices` caps each receipt at live settleable amount.
- **Same-day cash → CH_REV1 (`cash-holding-account.js`):** cash receipts debit CH_REV1 on the invoice's sale day, else CH_REV2; mixed groups refused. Consistent with the M5 partial fix.
- **Receipt date correction (bfe6e772, `PATCH /api/receipts/:id/date` → `updateReceiptDate`):** period lock on old+new dates, optimistic-concurrency 409, bank-in reversal guards both directions, CH_REV1 pool re-check under advisory locks, journal `entry_date` and CH_REV1↔CH_REV2 debit lines updated atomically. See N3/N4 for the two residuals.
- **Cancelled-invoice date editing (7621deef/46d0d95a):** `PUT /:id/datetime` now period-locks both stored and new dates (`invoices.js:5128-5135`); the cancelled-journal re-date is guarded `status='cancelled' AND manual_override=false` (`sales-journal.js:116-146`); nothing resurrectable (reports filter `posted`). This **resolves** the §4 "no cancelled check on /datetime" note as a deliberate feature.
- **Journal integration on invoice/adjustment pages + "related journals" (0e7f33fe, 02cad7ae, f9de5f73):** read-only. The TH journal PUT's source-owned detection moved from entry-type to linkage-based (`journal-entries.js:1189-1220`) — verified against dev DB that no genuine manual journal is caught and no linked journal escapes.
- **GT cheque_no machinery (7767167c):** `next-cheque-no` scans only `entry_type='C'` with a trailing-numeric regex, so the old TH PBE-suffix hijack can't recur; re-use warning ≥8 chars; ledgers read `COALESCE(line, header)`. Sound — see N5 nit.
- **GT advance payment / earlier received dates (8b0d1e48):** `assertPaymentNotBeforeInvoices` skipped only behind explicit `allow_advance_payment` UI confirmation; period lock and posting-date consistency (`entryDate = posting_date || received_date`) hold at create and confirm.
- **GT stored invoice_lines (2026-08-11 + RentalDetailsPage editor):** server-side exact-cents normalization vs `amount_before_tax`; edits 409-locked by `documentIdentityChanged` (incl. `linesChanged`) and refused once `einvoice_status='valid'`; proration in integer cents with residue on the last line ties exactly to the journal re-sync in the same transaction.
- **GT ERP/ledger statements, sales summary/report, payments pagination, JP multi-invoice + group payment cancellation:** read-only or JP-scoped; JP group cancel is well-guarded (expected-ids 409, cancelled-invoice and active-adjustment blocks, Overdue-preserving restore) and unreachable from TH pages (`onCancelPaymentGroup` passed only by JP `PaymentPage`).

### 7.4 New findings (2026-08-15)

| # | Severity | Finding | Citation | Status |
|---|----------|---------|----------|--------|
| N1 | **Medium** | **Cheque tender on a CASH bill 500s the whole invoice create.** The split-tender form offers Cheque for cash bills and `normalizeSaleTenders` accepts it, but `createReceipt` refuses pending cheques on CASH bills with a plain `Error` (no `.status`) → generic HTTP 500, invoice not created. Comments at `invoices.js:2519-2521, 2590-2591` describe a pending-keeps-balance-open behavior the guard makes unreachable. Fix: strip cheque from cash-bill tender options or surface 400 | `receipt-service.js:610-614`; `invoices.js:2660-2663`; `InvoiceFormPage.tsx:65-70, 1062` | **FIXED 2026-08-18** — both cash-bill guards now carry `status: 400` (clear message, no 500), and the TH invoice form no longer offers Cheque when the bill type is CASH (mirroring the PaymentForm convention). JP keeps cheque (no pending restriction there) |
| N2 | **Medium** | **GT opening-balances writes have no period lock or eligibility guards** (new router since the last audit). PUT `:accountCode`, PUT `/bulk`, DELETE validate only date format + account existence. An anchor dated before 2026-07-01 silently rewrites locked legacy history (anchors ignore all earlier posted lines); the pinned 500-row 2026-01-01 zero-sum set is editable/deletable; nothing blocks anchors on `DEBTOR` (double-count) or `BTFS` (breaks its blank/blank TB printing). TH's twin router is equally unguarded but GT's R8/verifier regime makes it sharper. Suggest: reject pre-open-date anchors + block control parents/`BTFS` | `greentarget/accounting/opening-balances.js:207, 336, 387`; auth at `index.js:397-402` | **FIXED 2026-08-18** — all three write paths run `assertAnchorWriteAllowed`: R8 period lock on the anchor date + a `DEBTOR`/`BTFS` block (CD_SD stays anchorable — its 2026-07-01 control anchor is legitimate); DELETE is period-locked but not account-blocked (deleting a forbidden anchor is always safe). Errors surface as 409/400 with their real messages. TH twin intentionally left unguarded: TH's 2026-01-01 anchor set is legitimately managed |
| N3 | **Medium (edge)** | **Receipt date correction rewrites detached journals.** `updateReceiptDate`'s posted-journal rewrite (`entry_date` + CH_REV1↔CH_REV2 debit-line swap) never checks `manual_override` — a hand-edited (detached) receipt journal is silently rewritten by a later date correction, contradicting the detach contract. The same commit series added the `manual_override=false` guard to the invoice redate path but not here. Requires prior detachment, hence edge | `receipt-service.js:1151-1171` vs guarded `sales-journal.js:116-146` | **FIXED 2026-08-18** — `updateReceiptDate` now refuses (409) when any group member's posted journal has `manual_override=true`, directing the user to re-date the journal from the Journal Entries page |
| N4 | Trivial | Date-moved receipt journals keep their original `posting_sequence`, so they can print out of order in the target day's ledger (TH within-day print order) | `receipt-service.js:1130-1172` | OPEN |
| N5 | Trivial | GT cheque prefill inherits the prefix of the max-suffix row; dev C cheques are keyed `PBB######` (TH-style), so the prefill computes `PBB…` rather than the `PB…` book the feature documents — a `PB`-prefix filter would pin it | `greentarget/accounting/journal-entries.js:449-475` | OPEN |
| N6 | Trivial | Credit-INVOICE split tender is not atomic across tenders: the frontend loops one `POST /api/payments` per tender; a mid-loop failure leaves earlier tenders posted (each individually balanced) with only a toast. No unbalanced journals; settlement can diverge from what was tendered | `InvoiceFormPage.tsx:695-734` | OPEN |
| N7 | Trivial | `POST /submit-invoices` never calls `normalizeSaleTenders` — a `payments` array there is silently ignored (all-cash treatment). No in-repo caller; external tooling only | `invoices.js:2835+` | OPEN |
| N8 | Trivial | Prorated GT invoice-line `unit_price = round(amount/quantity, 2)` can make `quantity × unit_price ≠ amount` on display (amount column stays authoritative; no ledger impact) | `sales-journal.js:332-356` | OPEN |
| N9 | Trivial | Shared `OpeningBalanceModal` now swallows server validation messages (`setError(t("Failed to save opening balance"))` instead of `err?.message`) — users lose the actual rejection reason | `src/components/Accounting/OpeningBalanceModal.tsx:104-111` | **FIXED 2026-08-18** — the modal now shows the server's message (`err instanceof Error ? err.message : fallback`), which the N2 guards rely on |
| N10 | Trivial | Two cancel dialogs don't pass the existing `isConfirming` prop, so their confirm buttons stay clickable during the async call (server rejects the second request harmlessly) | `JournalDetailsPage.tsx:567-583`; `BankInPage.tsx:819-830` | OPEN |

### 7.5 Suggested priority (not scheduled)

1. N1 (user-facing 500 on a form-offered option) — smallest real bug.
2. N2 (unguarded writes that can silently rewrite pinned GT history).
3. M2 (rounding drift on line edit) — oldest remaining data-consistency item.
4. N3 + M8 (detach-contract and stored-date guards — same guard pattern as existing fixes).
5. M6/pending-cheque class (M6, §4 dedupe, §4 legacy-confirm message) — one coherent piece of work.

---

## 8. Fix log (2026-08-18)

Implemented items 1–5 of §7.5 (per AGENTS.md rule 10, no build/typecheck run — user tests manually; all touched `.js` files verified with `node --check`, and the new settleable SQL was executed against the dev DB: invoices fully covered by a pending cheque report settleable 0, e.g. `2005054` balance 9,413 / pending 9,413 → 0).

- **N1 — `receipt-service.js:605-620` + `src/pages/Invoice/InvoiceFormPage.tsx:1062-1072`:** both cash-bill guards in `createReceipt` now throw `status: 400` errors (the invoice-create catch at `invoices.js:2653-2656` already honors `error.status`), and the tender method pill list filters out `cheque` when `paymenttype === "CASH"`. JP's own invoice form is untouched (JP accepts cheques on cash bills — no pending-cheque restriction, no journals).
- **N2 — `greentarget/accounting/opening-balances.js`:** new `assertAnchorWriteAllowed` (R8 `assertGreenTargetAccountingDateUnlocked` on the anchor date + `DEBTOR`/`BTFS` block) on PUT `/:accountCode`, PUT `/bulk` (validated before BEGIN) and DELETE (period lock only). Errors surface via `handleAnchorWriteError` as 409/400 with their messages. Also added the missing `as_of_date` ISO-format validation to PUT `/:accountCode`.
- **N9 (folded into N2) — `src/components/Accounting/OpeningBalanceModal.tsx`:** the save catch now surfaces the server's message (`err instanceof Error ? err.message : t("Failed to save opening balance")`), so the N2 guards are actually visible to the user.
- **M2 — `invoices.js` order-details + `jellypolly/invoices.js`:** both edit paths now select `rounding` and compute `total = subtotal + tax + rounding`, matching the create path, so a rounded invoice's totals keep adding up after an edit.
- **N3 — `receipt-service.js` `updateReceiptDate`:** refuses (409) the date correction when any group member's posted journal has `manual_override = true`, before any UPDATE, pointing the user at the Journal Entries page.
- **M8 — `greentarget/accounting/journal-entries.js` PUT:** now asserts the stored date as well as the submitted one (TH's exact comment and pattern).
- **M6 — net-out pending (user-chosen over block-entirely):** new exported `PENDING_SETTLEMENT_SQL` (pending receipt allocations + legacy standalone pending payments, `receipt_allocation_id IS NULL` preventing double-count) is subtracted, floored at 0, inside `SETTLEABLE_AMOUNT_SQL` — so the invoice list/detail `settleable_amount`, the `settleable=true` picker filter, the single-payment cap and the UI all net consistently. `getSettleableAmounts` (the JS mirror `lockInvoices` uses) applies the same netting and takes `excludeReceiptId` so a pending receipt never vetoes its own confirmation; only the confirm path passes it. Refusal messages name the uncleared cheque and its amount. UI: invoice detail also returns `pending_settlement`; the inline payment form caps at the netted amount and shows an amber "Uncleared cheque(s) already cover RM…" hint (i18n key added to `ms`/`zh-Hans` `invoice.json`). **Side effect:** the §4 double-submitted-pending-cheque hole is closed — a second identical pending cheque is refused because the first has netted the settleable amount. **Known remaining edge (documented, not fixed):** `applyOverpayment` still caps at raw `balance_due`, so a customer's held excess can be applied to a pending-covered invoice; the cheque confirmation would then fail. Low frequency; fix would be the same netting inside `applyOverpayment`.
- Changelog: 4 entries (2026-08-18) covering M6, N1, M2, and the N2/M8/N3 safeguard group.

Still OPEN after this pass: C2 (deferred by user), C7 (partially fixed; ~20 GT mounts + most JP mounts unauthenticated), M3, M4, M5 (residual), M7, U9, U10, N4–N8, N10, and the older §4 trivia not covered by the M6 netting.
